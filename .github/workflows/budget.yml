name: budget

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Token-budget check
        run: bash tests/budget/check-budget.sh --verbose
