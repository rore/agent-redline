# Invalid: changeRules.when uses an unknown enum value
version: 1

project:
  name: bad-when

zones:
  red:
    - path: src/main/java/**/domain/**
      reason: domain
      checkpoint: architecture-review

changeRules:
  - when: nonsense_condition
    action: warn

checkpoints:
  architecture-review:
    satisfiedBy:
      - codeownerApproval
